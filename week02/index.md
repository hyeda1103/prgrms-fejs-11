[← BACK](./../README.md)

# 2주차 TodoApp 업그레이드하기

- 학습일자: 2021-06-02 (수)

### 느낀 점
객체는 프로퍼티(상태)와 메서드(동작)의 집합체다. 생성자 함수인 경우 컴포넌트마다 상태 프로퍼티 `state`와 동작 메서드 `render`를 기본적으로 넣고, 파라메터로 전달받은 값이 변하는 경우, 상태를 새로 설정하기 위한 메서드 `setState`를 넣는 것을 기본으로 하여 컴포넌트를 작성해보자. 

### 읽으면 도움이 되는 글들

